<!-- The logic is decoupled from the html files.
 So the Thymeleaf tags are defined here only -->
<thlogic>
    <attr sel="#footerId"
          th:fragment="footerFragment"/>
    <attr sel="#created-by" th:text="|#{game.createdBy} João Domingues|" />
</thlogic>